---
import FormField from './FormField.astro';

interface Props extends astroHTML.JSX.InputHTMLAttributes {
  label?: string;
}

const { label, id, class: className, required, ...rest } = Astro.props;
const elementId = id || `input-${Math.random().toString(36).substr(2, 9)}`;
---

<FormField label={label} for={elementId} required={!!required} class={className || undefined}>
  <input
    id={elementId}
    required={!!required}
    class="w-full px-4 py-3.5 bg-[#F8F9FA] border-2 border-[#E9ECEF] rounded-lg font-body text-base text-[#343A40] transition-all outline-none focus:border-brand-blue focus:bg-white focus:ring-3 focus:ring-brand-blue/12 placeholder:text-[#868E96]"
    {...rest}
  />
</FormField>
